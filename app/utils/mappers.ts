import { CategoryHash } from '../types/common.types';

export const CATEGORY_HASH_TO_USER_LANGUAGE: Record<CategoryHash, string> = {
  '#tableSets': 'Table Sets',
  '#coffeeTables': 'Coffee Tables',
  '#mediaUnits': 'Media Units',
  '#sideTables': 'Side Tables',
};
